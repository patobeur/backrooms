import * as THREE from "../../vendor/three.module.min.js";
import {createItem} from "./item-registry.js";

export function snapshotItem(object){return Object.freeze({type:object.userData.type,userData:Object.freeze({...object.userData}),position:Object.freeze(object.position.toArray()),rotation:Object.freeze([object.rotation.x,object.rotation.y,object.rotation.z,object.rotation.order]),scale:Object.freeze(object.scale.toArray())});}

export function restoreItem(snapshot){const object=createItem(snapshot.type,{userData:{...snapshot.userData}});object.position.fromArray(snapshot.position);object.rotation.set(...snapshot.rotation);object.scale.fromArray(snapshot.scale);if(snapshot.userData.sips===0)object.traverse(child=>{if(child.material){child.material=child.material.clone();child.material.transparent=true;child.material.opacity=.15;child.material.color?.setHex(0xaaaaaa);}});if(snapshot.type==="plush"&&snapshot.userData.powered){if(snapshot.userData.fried){object.traverse(child=>{if(child.material){child.material=child.material.clone();child.material.color?.setHex(0x222222);}});object.add(new THREE.PointLight(0xff0000,3,10,2));}else{const glow=new THREE.PointLight(0xffd84a,.55,2.4,2);glow.userData.powerIndicator=true;object.add(glow);}}return object;}
